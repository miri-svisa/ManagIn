package com.example.managein;

//This department holds flags that vary depending on the situations of different departments
public class HelperUtil
{
    //A flag that keeps track of whether the user is connected to the camera or not
    public static boolean isCameraActivityBackPressed;
    //A flag that keeps the user from clicking Back
    public static boolean isInvoiceDetalesBackPressed;
    //A flag that keeps the user from changing the invoice information
    public static boolean isChangeDetails;
}
